<div class="title">
	<div class="container">
		<div class="fl">
			<h1 class="averia" title="{{recipe.title}}"{% if detail is defined %} itemprop="name"{% endif %}>{{recipe.title}}</h1>
			<ul class="recipemeta">
				{% include 'Design311WebsiteBundle:Shared:recipe-meta.html.twig' with {'white': true} %}
				<li class="hide4print"><i class="heart white sprite"></i>
				{% if recipe.likedBy|length == 0 %}
				Nog niemand heeft dit geproefd.
				{% else %}
				{{recipe.likedBy|length}} {{recipe.likedBy|length == 1 ? 'iemand vindt' : 'mensen vinden'}} dit lekker
				{% endif %}
				</li>
			</ul>
		</div>
		{% if detail is defined %}
		<div class="printrecipe"><a href="javascript:window.print()"><i class="sprite white printer"></i>Print deze pagina</a></div>
		{% endif %}
	</div>
</div>