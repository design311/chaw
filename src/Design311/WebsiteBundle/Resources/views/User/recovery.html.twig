{% extends 'Design311WebsiteBundle::layout.html.twig' %}

{% block content %}
<div class="container">
	<div class="onehalf">
		<h1 class="averia">Wachtwoord vergeten</h1>
			
		{% if error is not null %}
			<p class="message error">{{error}}</p>
		{% endif %}
		
		{{ form(form, {'attr': {'class': 'recoveryform'}} ) }}
	</div>
</div>

{% endblock %}
