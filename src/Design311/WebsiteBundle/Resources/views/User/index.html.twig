{% extends 'Design311WebsiteBundle::layout.html.twig' %}

{% block content %}
<div class="container">
	{% if app.user.description is null and app.user.recipes|length == 0 and app.user.dinners|length == 0 %}
		<div class="welcome">
			<h1 class="averia">Welkom bij Chaw</h1>
			<div class="onehalf">
				<p>Bedankt voor je aanmelding bij <a href="http://chaw.be">Chaw</a>, de community voor iedereen die graag lekker eet, iedereen dus? Maar ook wel een community voor de <em>proefgierigen</em> onder ons, ga eens bij een andere foodie eten en leer nieuwe recepten kennen.</p>
				<p>Ik wens je alvast heel veel lekkers toe, laat het smaken, geniet ervan met volle teugen.</p>
				<p><a href="{{ path('design311website_profile_view', {'username': 'yentl'}) }}">Yentl Bresseleers</a></p>
			</div>
			<div class="onehalf last">
				<p><strong>Bedankt, maar wat doe ik nu?</strong></p>
				<ol>
					<li><a href="{{ path('design311website_profile_edit') }}">Vul je profielgegevens aan &amp; upload een profielafbeelding</a></li>
					<li><a href="{{ path('design311website_recepten') }}">Ga op zoek naar een lekker recept</a> of <a href="{{ path('design311website_recepten_add') }}">deel zelf een recept</a></li>
					<li><a href="{{ path('design311website_dinners') }}">Neem deel aan een dinner</a> of <a href="{{ path('design311website_dinners_add') }}">organiseer een etentje bij je thuis</a></li>
				</ol>
			</div>
			<div class="clear"></div>
		</div>
	{% endif %}
	
	<div class="activities">
		<div class="one-third">
			<h2 class="averia">Dinners</h2>
			{% if dinners|length == 0 %}
			<p>Er zijn geen dinners voor je gepland, <a href="{{ path('design311website_dinners') }}">neem deel aan een dinner</a> of <a href="{{ path('design311website_dinners_add') }}">organiseer er zelf één</a>.</p>
			{% endif %}
			{% if dinners|length != 0 %}
				<ul class="dinnerlist">
				{% for dinner in dinners %}
				    <li>
						<a href="{{path('design311website_dinners_detail', {'permalink':dinner.permalink})}}"><img src="{{dinner.user.avatar | apply_filter('avatar_tiny') }}" alt="{{dinner.user.displayName}}"></a>
				    	<p class="title"><a href="{{path('design311website_dinners_detail', {'permalink':dinner.permalink})}}">{{dinner.title}}</a></p>
				    	<p class="dinnermeta">{{dinner.date|date('d-m-Y')}} - {{dinner.date|date('H:i')}} - {{dinner.address.city}}</p>
				    </li>
				{% endfor %}
				</ul>
			{% endif %}
		</div>
		<div class="two-third last">
			{% include 'Design311WebsiteBundle:Shared:homepage-recipes.html.twig' with {'recipes': app.user.savedRecipes, 'title': 'Bewaarde recepten', 'type': 'saved'} only %}

			{% include 'Design311WebsiteBundle:Shared:homepage-recipes.html.twig' with {'recipes': app.user.likedRecipes, 'title': 'Recepten die je lekker vind', 'type': 'liked'} only %}

			{% include 'Design311WebsiteBundle:Shared:homepage-recipes.html.twig' with {'recipes': app.user.recipes, 'title': 'Mijn recepten', 'type': 'recipes'} only %}

		</div>
	</div>
</div>
{% endblock %}